## Trias dependencies, tested with Python 3.10

# Core ML stack
torch>=2.2.0,<2.4
transformers>=4.40.0,<5.0
tokenizers>=0.19.0
accelerate>=0.30.0
datasets>=2.18.0

# Logging
wandb>=0.16.0

# Bio / sequence
biopython>=1.83,<2.0
viennarna>=2.6     # provides `import RNA` for MFE folding

# Notebooks and analysis (figures + benchmarking)
notebook>=7.0
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
scipy>=1.11
numpy>=1.26,<2.0
pandas>=2.0,<3.0
umap-learn>=0.5.7
fastdtw>=0.3.4
einops>=0.7,<1.0
scanpy>=1.10
gprofiler-official>=1.0 

# Training-only
# Required because BartConfig defaults to _attn_implementation="flash_attention_2".
# For CPU or non-flash GPU inference, override to "sdpa" or "eager" instead.
# flash-attn>=2.5.0